name: word_ribbon_poc
description: Word-style ribbon UI PoC - cross-platform Flutter app with platform-adaptive shells.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  fluent_ui: ^4.13.0
  macos_ui: ^2.2.2
  flutter_quill: ^11.5.0
  go_router: ^14.0.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
